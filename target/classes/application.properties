
spring.application.name=AnotherVault

# DataBase Configuration (PostgreSQL on a remote server)
spring.datasource.url=jdbc:postgresql://nefralene.beget.app:5432/default_db?targetServerType=master&ssl=false&sslmode=disable
spring.datasource.username=cloud_user
spring.datasource.password=gaD3pq%ikGxH
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Debug and Additional Properties
spring.jpa.hibernate.ddl-auto=update
logging.level.org.springframework.security=DEBUG

# S3 Configuration (for file uploads)
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging Configuration
logging.level.root=INFO
logging.level.com.VTB.AnotherVault=INFO
logging.file.name=logs/spring.log
